# Bootstrap-render fixture: the happy path.
# Exercises openspec/specs/argocd-day-zero-bootstrap "Bootstrap-identity subset
# read from cluster.yaml". target_revision is deliberately ABSENT so the same
# fixture also binds the `main` default scenario.
cluster:
  name: fixture-cluster
  overlay: prod
  endpoint: https://192.0.2.1:6443
repo:
  url: https://example.invalid/consumer-repo.git
talos:
  version: v1.13.9
kubernetes:
  version: v1.36.3
images:
  intel:
    cpu_vendor: intel
nodes:
  cp-1:
    ip: 192.0.2.10
    role: controlplane
    image: intel
