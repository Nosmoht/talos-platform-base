# Kubernetes Scope — Codex CLI Context
# Root scope: @../AGENTS.md (inherits §Hard Constraints, §Session Ritual, §MCP servers)

This file is loaded by Codex CLI when editing files under `kubernetes/`. It provides
path-scoped context approximating Claude Code's `paths:` auto-loading. Read the
linked rule files before editing in the listed contexts.

## Directory Map

| Path | Purpose |
|------|---------|
| `kubernetes/base/infrastructure/<app>/` | Shared Helm values and base Kustomize manifests |
| `kubernetes/overlays/homelab/infrastructure/<app>/` | Environment-specific ArgoCD Applications, values, CNPs |
| `kubernetes/overlays/homelab/apps/` | Application-tier ArgoCD Application CRs |
| `kubernetes/overlays/homelab/projects/` | AppProject CRs (ArgoCD RBAC boundaries) |
| `kubernetes/bootstrap/` | One-time bootstrap manifests — do NOT hand-edit; managed by Makefile targets |

Component directory names must match their ArgoCD Application name exactly (e.g. `kube-prometheus-stack/`, not `monitoring/`).

## Domain Rules by Edit Context

Before editing, read the applicable rule file:

| Context | Rule file |
|---------|-----------|
| ArgoCD Applications, sync issues, drift | `.claude/rules/argocd-operations.md` |
| HTTPRoute, TLSRoute, Gateway, GatewayClass | `.claude/rules/cilium-gateway-api.md` |
| `cnp-*.yaml`, `ccnp-*.yaml` files | `.claude/rules/cilium-network-policy.md` |
| Kustomize overlays, ksops generators, sync-waves | `.claude/rules/kubernetes-gitops.md` |
| Helm values, resource limits, labels | `.claude/rules/manifest-quality.md` |
| Grafana dashboards, PrometheusRule, ServiceMonitor | `.claude/rules/monitoring-observability.md` |
| LINSTOR StorageClass, Piraeus resources | `.claude/rules/linstor-storage-guardrails.md` |

## Hard Constraints (inline summary — canonical in `../AGENTS.md §Hard Constraints`)

- **No `kind: Ingress`** — use `HTTPRoute`/`TLSRoute` (Gateway API)
- **No `kind: Endpoints`** — use `EndpointSlice`
- **No `kubectl apply` on ArgoCD-managed resources** — commit to git, push, let ArgoCD sync
- **SOPS files**: `*.sops.yaml` must contain `sops:` key and recipient backend; never commit plaintext
- **Labels**: all resources must carry `app.kubernetes.io/{name,instance,component,part-of,managed-by}`
- **CNP naming**: `cnp-<component>.yaml` (namespace-scoped), `ccnp-<description>.yaml` (clusterwide)
- **PNI first**: new consumer-to-platform connectivity must use PNI labels before ad-hoc CNPs
  - Required: `platform.io/network-interface-version: v1` + `platform.io/network-profile: restricted|managed|privileged`

## Excluded from Search

- `kubernetes/overlays/homelab/infrastructure/kubevirt/resources/kubevirt-operator.yaml` — vendor-generated (7669 lines)
- `kubernetes/overlays/homelab/infrastructure/kubevirt-cdi/resources/cdi-operator.yaml` — vendor-generated (5486 lines)
- `kubernetes/bootstrap/cilium/cilium.yaml` — generated by `make cilium-bootstrap` (2339 lines); never hand-edit
