apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # No namespace.yaml — vault-config-operator is a secondary tenant of
  # the vault namespace, which is owned by vault-operator. See ADR
  # docs/adr-namespace-ownership-rendered-manifests.md.
  - _rendered/manifests.yaml
  - _rendered/crds.yaml
