## 1. Guard

- [x] 1.1 Decide the IPv6 half of `cilium_k8s_service_host` by a `cidrhost`
      round trip instead of a character class
- [x] 1.2 Bind the narrowing red-green: legs for a non-parsing hex-colon
      string, an oversized group, a nine-group string, a non-canonical spelling
      and an IPv4-embedded literal

## 2. Schema

- [x] 2.1 State the host pattern as a conservative pre-filter in the spec
- [x] 2.2 Leave the pattern itself unchanged — it already rejects only values
      the module also rejects
