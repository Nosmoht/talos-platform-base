schema: spec-driven

# Operational knowledge lives in AGENTS.md (canonical, tool-agnostic) — this
# context block stays a pointer, never a parallel source of truth.
context: |
  Read AGENTS.md (repo root) first: repository purpose, Hard Constraints,
  validation commands, commit conventions. Deep reference: knowledge/index.md.
  Specs in openspec/specs/ cover consumer-facing platform behavior only;
  repo-internal QA is documented in knowledge/, not spec'd.

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits
#     Domain: e-commerce platform

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
