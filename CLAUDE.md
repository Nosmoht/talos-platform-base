# talos-platform-base — Claude Code Memory

@AGENTS.md

## Claude-Code Delta

`AGENTS.md` above is the whole contract; this file adds only what is specific
to Claude Code.

- The spec-driven workflow runs through the `/opsx:*` slash commands from the
  committed `.claude/commands/` tree — see AGENTS.md §Spec-Driven Development
  and §Tool Notes for what ships and what does not.
- Deep reference: [`knowledge/index.md`](knowledge/index.md), the OKF v0.2
  bundle. The root-level orientation files cover governance and scope;
  everything else is indexed there.
