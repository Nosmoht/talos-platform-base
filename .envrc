# Activates the Devbox environment on cd into this repo.
# Prerequisite: direnv installed + shell hook set (https://direnv.net/docs/hook.html).
# First activation: `direnv allow`.

eval "$(devbox generate direnv --print-envrc)"
