# INTENTIONALLY INVALID cluster.yaml — negative fixture for lint-cluster-yaml.sh
# (issue #136 Task 3). Valid in every respect EXCEPT one node carries
# role: master, which the schema's node.role enum (controlplane | worker) rejects.
# CI asserts this file is REJECTED; relax the role enum in
# docs/schemas/cluster.schema.json and the negative CI step turns red.
# Not a runnable cluster — RFC5737 documentation IPs.
cluster:
  name: bad-cluster
  endpoint: https://bad-cluster-api.example.com:6443
repo:
  url: https://github.com/example/bad-cluster.git
talos:
  version: v1.12.6
kubernetes:
  version: v1.35.0
images:
  standard:
    architecture: amd64
    cpu_vendor: intel
    extensions: []
nodes:
  - hostname: cp-1
    ip: 192.0.2.11
    role: master # INVALID: schema enum is controlplane | worker
    image: standard
    hardware_capabilities: []
