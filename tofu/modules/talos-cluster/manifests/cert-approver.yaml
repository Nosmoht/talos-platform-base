# kubelet-serving-cert-approver — VENDORED upstream manifest (substrate seed).
#
# Source : https://github.com/alex1989hu/kubelet-serving-cert-approver
#          deploy/standalone-install.yaml @ tag v0.11.0
# Upstream file SHA256 (deploy/standalone-install.yaml): 310be96fc85ea1016daddc3d609ebb0042130f98fec7829240c06642cc9663f4
# Vendored: 2026-06-30. Seeded as a controlplane cluster.inlineManifest by
#           tofu/modules/talos-cluster (local.cert_approver_controlplane_patch).
#
# Modifications applied to the upstream file (re-apply on every version bump):
#   1. The upstream `kind: Namespace` document is REMOVED — the namespace is
#      seeded separately with the base PSA/label floor (local.cert_approver_namespace_labels).
#   2. The container image is DIGEST-pinned (@sha256) for supply-chain integrity.
#   3. `automountServiceAccountToken: true` set explicitly on the pod spec (the
#      approver requires its SA token to call the CSR-approval API).
#   4. `imagePullPolicy: Always` -> `IfNotPresent`: with a digest pin the content is
#      already immutable, so Always buys no integrity but forces a ghcr.io round-trip
#      on every pod start — a bootstrap / CP-reboot availability foot-gun. IfNotPresent
#      reuses the cached, digest-verified bytes.
#   5. The six app.kubernetes.io labels (name/instance/version/component/part-of/
#      managed-by) are set on every object's metadata AND the pod template. Upstream
#      ships only name+instance; AGENTS.md requires all six on all resources, and this
#      seed is delivered outside the kustomize render/label gate, so the labels are
#      applied here at the source. Selectors keep the stable name+instance subset
#      (a Deployment selector is immutable; a version label in a selector is an
#      anti-pattern). Values match local.cert_approver_namespace_labels.
#
# NOTE (upstream-retained, not modified): the `events:` RoleBinding lands in namespace
# `default` (upstream design) — the approver emits its audit Events into `default`,
# not into its own namespace; operators triaging approve/deny decisions look there.
#
# Security model (see docs/adr-0013): isRequestConform validates Org==system:nodes,
# CN prefix system:node:, CN==CSR.username, no email/URI SANs, usages — but does
# NOT bind requested DNS/IP SANs to the requesting node. SAN-to-node binding is a
# consumer-cluster Kyverno defense-in-depth obligation (ADR-0004 §kubelet-serving).
# RBAC `approve` is signer-restricted to kubernetes.io/kubelet-serving (verified).
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/component: cert-approver
    app.kubernetes.io/instance: kubelet-serving-cert-approver
    app.kubernetes.io/managed-by: opentofu
    app.kubernetes.io/name: kubelet-serving-cert-approver
    app.kubernetes.io/part-of: talos-platform-base
    app.kubernetes.io/version: 0.11.0
  name: kubelet-serving-cert-approver
  namespace: kubelet-serving-cert-approver
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/component: cert-approver
    app.kubernetes.io/instance: kubelet-serving-cert-approver
    app.kubernetes.io/managed-by: opentofu
    app.kubernetes.io/name: kubelet-serving-cert-approver
    app.kubernetes.io/part-of: talos-platform-base
    app.kubernetes.io/version: 0.11.0
  name: certificates:kubelet-serving-cert-approver
rules:
- apiGroups:
  - certificates.k8s.io
  resources:
  - certificatesigningrequests
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - certificates.k8s.io
  resources:
  - certificatesigningrequests/approval
  verbs:
  - update
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
- apiGroups:
  - certificates.k8s.io
  resourceNames:
  - kubernetes.io/kubelet-serving
  resources:
  - signers
  verbs:
  - approve
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/component: cert-approver
    app.kubernetes.io/instance: kubelet-serving-cert-approver
    app.kubernetes.io/managed-by: opentofu
    app.kubernetes.io/name: kubelet-serving-cert-approver
    app.kubernetes.io/part-of: talos-platform-base
    app.kubernetes.io/version: 0.11.0
  name: events:kubelet-serving-cert-approver
rules:
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/component: cert-approver
    app.kubernetes.io/instance: kubelet-serving-cert-approver
    app.kubernetes.io/managed-by: opentofu
    app.kubernetes.io/name: kubelet-serving-cert-approver
    app.kubernetes.io/part-of: talos-platform-base
    app.kubernetes.io/version: 0.11.0
  name: events:kubelet-serving-cert-approver
  namespace: default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: events:kubelet-serving-cert-approver
subjects:
- kind: ServiceAccount
  name: kubelet-serving-cert-approver
  namespace: kubelet-serving-cert-approver
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/component: cert-approver
    app.kubernetes.io/instance: kubelet-serving-cert-approver
    app.kubernetes.io/managed-by: opentofu
    app.kubernetes.io/name: kubelet-serving-cert-approver
    app.kubernetes.io/part-of: talos-platform-base
    app.kubernetes.io/version: 0.11.0
  name: kubelet-serving-cert-approver
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: certificates:kubelet-serving-cert-approver
subjects:
- kind: ServiceAccount
  name: kubelet-serving-cert-approver
  namespace: kubelet-serving-cert-approver
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: cert-approver
    app.kubernetes.io/instance: kubelet-serving-cert-approver
    app.kubernetes.io/managed-by: opentofu
    app.kubernetes.io/name: kubelet-serving-cert-approver
    app.kubernetes.io/part-of: talos-platform-base
    app.kubernetes.io/version: 0.11.0
  name: kubelet-serving-cert-approver
  namespace: kubelet-serving-cert-approver
spec:
  ports:
  - name: metrics
    port: 9090
    protocol: TCP
    targetPort: metrics
  selector:
    app.kubernetes.io/instance: kubelet-serving-cert-approver
    app.kubernetes.io/name: kubelet-serving-cert-approver
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: cert-approver
    app.kubernetes.io/instance: kubelet-serving-cert-approver
    app.kubernetes.io/managed-by: opentofu
    app.kubernetes.io/name: kubelet-serving-cert-approver
    app.kubernetes.io/part-of: talos-platform-base
    app.kubernetes.io/version: 0.11.0
  name: kubelet-serving-cert-approver
  namespace: kubelet-serving-cert-approver
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: kubelet-serving-cert-approver
      app.kubernetes.io/name: kubelet-serving-cert-approver
  template:
    metadata:
      labels:
        app.kubernetes.io/component: cert-approver
        app.kubernetes.io/instance: kubelet-serving-cert-approver
        app.kubernetes.io/managed-by: opentofu
        app.kubernetes.io/name: kubelet-serving-cert-approver
        app.kubernetes.io/part-of: talos-platform-base
        app.kubernetes.io/version: 0.11.0
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - preference:
              matchExpressions:
              - key: node-role.kubernetes.io/control-plane
                operator: DoesNotExist
            weight: 100
      containers:
      - args:
        - serve
        env:
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        image: ghcr.io/alex1989hu/kubelet-serving-cert-approver:0.11.0@sha256:f17017b53983fe93f5a07ef85b2e448b9e4be848d45f05c325c8f2c77e1171c4
        imagePullPolicy: IfNotPresent
        livenessProbe:
          httpGet:
            path: /healthz
            port: health
          initialDelaySeconds: 6
        name: cert-approver
        ports:
        - containerPort: 8080
          name: health
        - containerPort: 9090
          name: metrics
        readinessProbe:
          httpGet:
            path: /readyz
            port: health
          initialDelaySeconds: 3
        resources:
          limits:
            cpu: 250m
            memory: 32Mi
          requests:
            cpu: 10m
            memory: 16Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          privileged: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
      priorityClassName: system-cluster-critical
      securityContext:
        fsGroup: 65534
        runAsGroup: 65534
        runAsUser: 65534
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: kubelet-serving-cert-approver
      automountServiceAccountToken: true
      tolerations:
      - effect: NoSchedule
        key: node.cloudprovider.kubernetes.io/uninitialized
        operator: Exists
      - effect: NoSchedule
        key: node-role.kubernetes.io/master
        operator: Exists
      - effect: NoSchedule
        key: node-role.kubernetes.io/control-plane
        operator: Exists
