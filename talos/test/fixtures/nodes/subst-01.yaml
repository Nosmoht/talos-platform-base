# nodes/subst-01.yaml — per-node fixture for valid-substitution.yaml test.
# Supplies machine.network.bridge.nic so the kubevirt-networking capability's
# NIC_NAME placeholder resolves.
machine:
  install:
    disk: /dev/sda
  network:
    hostname: subst-01
    bridge:
      nic: eth0
