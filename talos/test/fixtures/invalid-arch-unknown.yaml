# invalid-arch-unknown.yaml — triggers:
# FAIL [n.cp-01]: arch 'riscv64' not in cluster.yaml architectures
roles:
  controlplane:
    description: "Control-plane node"

architectures:
  amd64:
    talos-arch: amd64
    compatible-infrastructure-platforms:
      - metal

infrastructure-platforms:
  metal:
    installer-profile: metal

hardware-platforms:
  intel-generic:
    vendor: Intel
    model: "Generic x86-64"

hardware-capabilities:
  base-server:
    description: "Standard compute"
    requires_features:
      - nvidia-gpu

nodes:
  - name: cp-01
    role: controlplane
    arch: riscv64
    infrastructure-platform: metal
    hardware-platform: intel-generic
    hardware-capabilities:
      - base-server
    ip: 192.0.2.11
    nic: eth0
